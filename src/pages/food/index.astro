---
import AppLayout from '../../layouts/AppLayout.astro';
import InfoDirectory from '../../islands/InfoDirectory.svelte';
import { buildInfoPageData } from '../../lib/infoPage';

const { cards, places, mapConfig, leafletBase, showMap } = await buildInfoPageData('food');
---

<AppLayout title="Еда">
  <header class="page-header">
    <h1>Еда</h1>
    <p>Заведения по городам: <code>content/food/beijing.md</code>, <code>shanghai.md</code>.</p>
  </header>
  <InfoDirectory client:load {cards} {places} {mapConfig} {leafletBase} {showMap} />
</AppLayout>
